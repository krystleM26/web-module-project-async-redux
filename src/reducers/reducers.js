const initialState = {
  
        weather: 
        { id: '', main: '', descriptions: '' },
  isFetching: "true",
  error: "Error Message"
};




export const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}


export default reducer
